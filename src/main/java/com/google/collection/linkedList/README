手写LinkedList:

数据结构：
数组 链表 二叉树 红黑树

数组特性：连续性 查询快
数组的数据删除原理：把后面的元素向前移动，将最后的一个元素变为空
指定下标添加原理：指定位置后面的元素向后移动，然后添加数据

链表结构：
节点 包含：节点内容，下一个节点内容
单向链表 查询原理：需要上一个节点的Next,一直查到当前位置

双向链表
Node1  Node2 Node3
如何删除：假如删除Node2
1.获取删除元素的上一个节点，和下一个节点
2.将Node1的下一个节点改为Node2
3.将Node3的上一个节点改为Node1

LinkedList.class中
定义了Node first; Node last
为什么需要有first? 为了查询 从first开始到下标结束
为什么需要有last?  添加元素从链表尾部开始
用到了二分查找



